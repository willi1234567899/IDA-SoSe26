# Optional local fonts

For fully offline demos, place Source Sans 3 `.woff2` files here and uncomment the
`@font-face` rules in `../styles.css`.

Until then the app loads Source Sans 3 via Google Fonts in `styles.css`.
