# Static assets for the Streamlit app

- `styles.css` – light-blue theme, Source Sans Pro / Source Sans 3
- `logo.svg` – Group 38 / Quality Science style logo
- `fonts/` – optional local font files for fully offline runs

The app loads CSS and logo via relative paths from the project root.
